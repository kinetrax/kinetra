{
  "name": "kinetrax-website",
  "version": "1.0.0",
  "description": "KinetraX website",
  "scripts": {
    "config": "node generate-config.js",
    "build": "npm run config"
  },
  "engines": {
    "node": ">=2.0.0"
  }
}
